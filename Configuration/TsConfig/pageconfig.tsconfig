mod{
  qcRedirects{
    csvExport {
      filename {
        // This date will added to the exported file name
        dateFormat = YmdHi
      }
      // Csv parameters
      separator = ;
      enclosure = "
      escape = \\
    }
  }
}
