mod{
  qcRedirects{
    csvExport {
      // Csv parameters
      separator = ,
      enclosure = "
      escape = \\
      // @Todo : add this options when exporting
      recordOrder = DESC
    }
  }
}
